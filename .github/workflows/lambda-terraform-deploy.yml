name: lambda-terraform-deploy

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to Apply (dev/stage/prod)"
        required: true
        default: "dev"

permissions:
  id-token: write # required for OIDC
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      TF_IN_AUTOMATION: true
      TF_INPUT: false
      AWS_REGION: us-east-1
      TF_VAR_environment: prod
      TF_VAR_aws_region: us-east-1

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Package Lambda
        run: |
          chmod +x lambda_src/package_lambda.sh
          cd lambda_src && ./package_lambda.sh

      # -----------------------------------
      # ASSUME AWS IAM ROLE VIA GITHUB OIDC
      # -----------------------------------
      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ env.AWS_REGION }}
          role-to-assume: arn:aws:iam::436812234045:role/OIDC
          role-session-name: gha-lambda-terraform-apply

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.5"
          terraform_wrapper: false

      - name: Terraform init
        run: terraform init

      - name: Terraform validate
        run: terraform validate

      - name: Terraform apply
        if: true
        run: terraform apply -auto-approve
